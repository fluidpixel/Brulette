<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.brewlette"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- GCM requires a Google account. -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- Keeps the processor from sleeping when a message is received. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- This app has permission to register with GCM and receive message -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- MODIFICATION REQUIRED - Replace "com.urbanairship.push.sample" with your package name -->
    <permission
        android:name="com.android.brewlette.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.android.brewlette.permission.C2D_MESSAGE" />
    <!-- The two elements above ensure that only this application can receive the messages and registration result -->
    <!-- END Urban Airship Required Permissions -->


    <!-- OPTIONAL Urban Airship Settings -->
    <!-- REQUIRED FOR LOCATION -->
    <!-- Use ACCESS_COARSE_LOCATION if GPS access is not necessary -->
    <!-- uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" / -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.android.brewlette.Brewlette"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="views.LoginView"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="views.RegisterView"
            android:label="@string/register" />
        <activity
            android:name="views.HomeView"
            android:label="@string/home" />
        <activity
            android:name="views.team.CreateTeamView"
            android:label="@string/title_activity_create_team_view" >
        </activity>
        <activity
            android:name="views.team.TeamsView"
            android:label="@string/title_activity_teams_view" >
        </activity>
        <activity
            android:name="views.team.TeamView"
            android:label="@string/title_activity_team_view" >
        </activity>
        <activity
            android:name="views.brew.BrewsView"
            android:label="@string/title_activity_brews_view" >
        </activity>
        <activity
            android:name="views.brew.BrewView"
            android:label="@string/title_activity_brew_view" >
        </activity>
        <activity
            android:name="views.brew.CreateBrewView"
            android:label="@string/title_activity_create_brew">
        </activity>
        <activity
            android:name="views.brew.SelectBrewView"
            android:label="@string/title_activity_select_brew_view"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="views.brew.SelectSweeterView"
            android:label="@string/title_activity_select_sweeter_view"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="views.team.StartRoundView"
            android:label="@string/title_activity_start_round_view" >
        </activity>
        <activity
            android:name="views.brew.SelectSmootherView"
            android:label="@string/title_activity_select_smoother_view"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="views.brew.SelectApparatusView"
            android:label="@string/title_activity_select_apparatus_view"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="views.brew.SelectMethodView"
            android:label="@string/title_activity_select_method_view"
            android:windowSoftInputMode="stateHidden" >
        </activity>
        <activity
            android:name="views.profile.ProfileView"
            android:label="@string/title_activity_profile_view" >
        </activity>
        <activity
            android:name="views.round.RoundView"
            android:label="@string/title_activity_round_view" >
        </activity>

        <!-- REQUIRED for Urban Airship GCM -->
        <receiver android:name="com.urbanairship.CoreReceiver" />
        <receiver
            android:name="com.urbanairship.push.GCMPushReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <!-- MODIFICATION REQUIRED - Use your package name as the category -->
                <category android:name="com.android.brewlette" />
            </intent-filter>
            <!-- REQUIRED for detecting when the application is upgraded so it can request a new GCM ID -->
            <intent-filter>
                <action android:name="android.intent.action.com.android.brewlette" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.urbanairship.push.PushService"
            android:label="Push Notification Service" />
        <!-- REQUIRED only with UA Android lib version 2.1.4 and earlier. Do not include PushWorkerService with UA Android lib version 3.0.0+ since it has been deprecated -->
        <service
            android:name="com.urbanairship.push.PushWorkerService"
            android:label="Push Notification Worker Service" />
        <service
            android:name="com.urbanairship.analytics.EventService"
            android:label="Event Service" />

        <!-- This is required for persisting preferences related to push and location -->
        <!-- MODIFICATION REQUIRED - Use your package name + ".urbanairship.provider" in the android:authorities -->
        <provider
            android:name="com.urbanairship.UrbanAirshipProvider"
            android:authorities="com.android.brewlette.urbanairship.provider"
            android:exported="false"
            android:multiprocess="true" />
        <!-- END OF REQUIRED ITEMS -->


        <!-- OPTIONAL (for segments support) -->
        <service
            android:name="com.urbanairship.location.LocationService"
            android:label="Segments Service" />

        <!-- OPTIONAL, if you want to receive push, push opened and registration completed intents -->
        <!-- Replace the receiver below with your package and class name -->
        <receiver android:name="com.android.brewlette.NotificationReceiver" />

        <activity
            android:name="views.round.StartRoundView"
            android:label="@string/title_activity_start_round" >
        </activity>
    </application>

</manifest>